rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // FIXED: Development rules - allow authenticated users to upload + read
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
